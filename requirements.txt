# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.24.0
opencv-python>=4.7.0
matplotlib>=3.7.0
Pillow>=9.5.0

# SAM2 specific
hydra-core>=1.3.0
iopath>=0.1.10
omegaconf>=2.3.0
submitit>=1.4.5

# For training (optional)
tensordict>=0.1.0
pandas>=2.0.0

# Progress bars
tqdm>=4.65.0

# MUSt3R integration (optional - for geometry fusion)
# Note: Install MUSt3R separately from https://github.com/naver/must3r
# pip install git+https://github.com/naver/must3r.git

# SAM2 installation
# git submodule update --init --recursive
# cd sam2_repo && pip install -e .

